[core]
	editor = vim
	pager = less -r
	excludesfile = ~/.gitignore
[color]
	ui = auto
[alias]
	co = checkout
	ci = commit
	st = status
	b = branch -vv
	ll = log --oneline
[include]
	path = ~/.gitconfig.local
[credential]
	helper = store --file ~/.git-credentials
